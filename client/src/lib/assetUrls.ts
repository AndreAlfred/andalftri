/** Content-versioned so the browser may cache the GLB immutably. */
export const MEDALLION_URL = "/models/medallion-v2.glb";

/** Same decoder Three ships, hosted locally to avoid a gstatic startup hop. */
export const DRACO_DECODER_PATH = "/draco-v1/";

/** Explicitly local so Troika never discovers its default font/CDN on hover. */
export const SCENE_LABEL_FONT_URL = "/fonts/ChivoMono-VariableFont_wght.ttf";
